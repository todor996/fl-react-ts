export type photoObject = {
    src: string,
    width: number;
    height: number;
}

export type imageStyle = {
    margin: any,
    display: string,
    position?: any,
    left?: number;
    top?: number;
};
